#notes
  = render "shared/navigation"
  .actions
    = link_to "Delete", [:delete, @note], {method: :delete, data: {confirm: "Are you sure?"}}
  .note
    span.updated= @note.updated_at.strftime("%Y-%m-%dT%H:%M:%S")
    pre
      = @note.content
